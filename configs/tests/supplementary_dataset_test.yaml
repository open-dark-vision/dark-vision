dataset:
  path: "data/Supplementary"
  batch_size: 16
  pin_memory: False
  num_workers: 0

  test_transform:
    additional_targets:
    pipeline:
      - name: resize
        height: 512
        width: 512
      - name: to_tensor
