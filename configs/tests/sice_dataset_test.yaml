dataset:
  path: "data/SICE"
  batch_size: 16
  val_size: 0.1

  max_exposure_ratio: 1.0
  train_pair_selection_method: "random_target"
  test_pair_selection_method: "halfexp_target"

  pin_memory: False
  num_workers: 0
