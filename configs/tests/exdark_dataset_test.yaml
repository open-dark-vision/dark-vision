dataset:
  path: "data/ExDark"
  batch_size: 16
  val_size: 0.05
  pin_memory: False
  num_workers: 0

  train_transform:
    additional_targets:
    pipeline:
      - name: resize
        height: 512
        width: 512
      - name: to_tensor

  test_transform:
    additional_targets:
    pipeline:
      - name: resize
        height: 512
        width: 512
      - name: to_tensor
